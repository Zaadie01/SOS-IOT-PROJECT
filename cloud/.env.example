# Docker Compose — server-level variables
# Copy to cloud/.env on the server and fill in your GitHub username.

# Your GitHub username (lowercase) — used for GHCR image names
REGISTRY_OWNER=your-github-username

# Set automatically by deploy.sh (sha-xxxxxxx or latest)
IMAGE_TAG=latest
